{% extends 'base.html.twig' %}

{% block title %}{{Regions.regionstitrer}} {% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>{{Regions.regionstitrer}}</h1>
        <p>{{Regions.descregionsr|raw}}</p>
        {% if app.user %}
        <a href={{ asset("/editeur_regions/"~ Regions.id)}}><button type="button" class="btn btn-secondary">Rajouter/modifier des informations</button></a>
        {% endif %}
        {% if not app.user %}
        <p>Vous pouvez apportez vos connaissance sur cette région en vous inscrivant <a href={{ asset("/inscription/")}}>ici</a> ou en vous connectant <a href={{ asset("/connexion/")}}>ici</a></p>
        {% endif %}
    </div>
</div>
{% endblock %}
